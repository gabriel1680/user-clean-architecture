import AuthMiddleware from "../../../../src/adapters/authentication/middlewares/AuthMiddleware";

describe("Unit Tests of AuthMiddleware", () => {
  it("shout be able to valid the auth token", () => {
    // const authMiddleware = new AuthMiddleware()
  });
});